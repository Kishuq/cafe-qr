# Copy to .env to override defaults (never commit a real .env)
# Port the server listens on
PORT=3000
# Owner dashboard PIN (4+ chars). If unset, the PIN stored in data/db.json is used (default 1234 — change it!).
ADMIN_PIN=
